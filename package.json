{"name": "almondcove",
"version": "1.1.0",
  "scripts": {
    "tsserve": "tsc",
    "serve": "dotnet watch",
    "arrangets": "tsc && copyfiles -u 1 Scripts/**/*.ts wwwroot/assets/js",
    "publish64": "dotnet publish -c Release -r win-x64 --self-contained false"
  }
}
